// Convenience re-export — the actual nav structure lives in
// src/config/navigation.config.js so it sits alongside other site config.
export { mainNav, footerNav } from "@/config/navigation.config";
